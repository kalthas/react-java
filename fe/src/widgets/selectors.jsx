const getApplication = (state) => state.get('application');
const getViewContainer = (state) => state.get('viewContainer');
const getViews = (state) => state.get('views');

export {
    getApplication,
    getViewContainer,
    getViews
}